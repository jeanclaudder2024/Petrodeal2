import { URL } from "url";
import {
  GotenbergClient,
  GotenbergClientClass,
  GotenbergClientFunction,
  Request,
  Source,
} from "./_types";
declare export var gotenberg: (
  url: string | Buffer | URL,
  client?:
    | GotenbergClient
    | GotenbergClientFunction
    | GotenbergClientClass
    | { [key: string]: any },
  config?: { [key: string]: any }
) => (source: Source) => Request;
