# Environment Variables - copy to .env in project root and/or in document-processor/
# Used by: root main.py (port 8000), document-processor (port 5000), and React (Vite)

# ============ Supabase (required for Python APIs) ============
# Get from: Supabase Dashboard -> Project Settings -> API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-key

# Optional: service role key for document-processor (admin / bypass RLS)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============ Document Processor (Python) - optional ============
# OPENAI_API_KEY=sk-...

# ============ React / Vite ============
# Local dev: leave unset or use http://localhost:5000
# VPS production: set to /api so the app calls your domain/api (nginx proxies to Python)
# VITE_DOCUMENT_API_URL=/api
